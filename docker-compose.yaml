services:
  mongodb:
    image:mongodb:v1
network:
  default:
    driver: bridge
    name: roboshop
    external: false     #I f network is not created this will create the network.
                        ## if network is already created you should change it as true
